<form method="post" action="{{ path('app_distribution_delete', {'id': distribution.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ distribution.id) }}">
    <button class="btn">Delete</button>
</form>
