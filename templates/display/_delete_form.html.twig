<form method="post" action="{{ path('app_display_delete', {'id': display.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ display.id) }}">
    <button class="btn">Delete</button>
</form>
