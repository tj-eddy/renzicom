{% extends 'base.html.twig' %}

{% block title %}{{ 'stock.form.title_edit'|trans }}{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header">
            <h4 class="card-title">{{ 'stock.form.title_edit'|trans }}</h4>
        </div>
        <div class="card-content">
            <div class="card-body">
                {{ include('stock/_form.html.twig', {'button_label': 'Create'}) }}
            </div>
        </div>

    </div>
{% endblock %}
