{% extends 'base.html.twig' %}

{% block title %}Rack{% endblock %}

{% block body %}
    <h1>Rack</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rack.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ rack.name }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ rack.image }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ rack.address }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_rack_index') }}">back to list</a>

    <a href="{{ path('app_rack_edit', {'id': rack.id}) }}">edit</a>

    {{ include('rack/_delete_form.html.twig') }}
{% endblock %}
